const LoadMoreBtn = ({ onClick }) => (
    <div className="load-more-container">
      <button className="load-more-btn" onClick={onClick}>Load more</button>
    </div>
  );
  
  export default LoadMoreBtn;